version: '3.8'

services:
  hic-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ${DATA_DIR}:/data
      - .:/app
    environment:
      DATA_DIR: /data
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
